   #!/bin/bash
   
   docker ps -a
   docker stop $(docker ps -a -q)
   docker rm $(docker ps -a -q)

   stsCron=1
   until [ $stsCron = 0 ]
   do 
       if [ $stsCron != 0 ] ; then
           sh _commCron.sh &
       fi
       sleep 1
   done
